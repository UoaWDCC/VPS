{
  "name": "vps",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && yarn run dev\" \"cd backend && yarn run dev\"",
    "setup": "yarn install && concurrently \"cd frontend && yarn install\" \"cd backend && yarn install\"",
    "contributors:add": "all-contributors add",
    "contributors:generate": "all-contributors generate"
  },
  "dependencies": {
    "react-router-dom": "^7.9.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "all-contributors-cli": "^3.1.1",
    "concurrently": "^9.1.2",
    "daisyui": "^5.3.2",
    "tailwindcss": "^4.1.14"
  }
}
